<% products.forEach(product => { %>
    <% if (categoria == "men" && product.category.id == 1) { %>
        <div class="col-xs-12 col-sm-6 col-md-3">
            <article>
                <a href="/product/detail/<%= product.id %>">
                <img src="/images/<%= product.images[0].name %>" alt="<%= product.name %>" class="pd_img-list">
                </a>
                <div class="pd_main-article">
                    <p class="pd_categoria"><%= product.brand.name %> </p>
                    <p class="pd_descripciones">
                        <a href="/product/detail/<%= product.id %>"> <%= product.name %></a>
                    </p>
                    <p class="pd_precios">$<%= product.price %></p>
                </div>
            </article>
        </div>
    <% } else if (categoria == "women" && product.category.id == 2) { %>
        <div class="col-xs-12 col-sm-6 col-md-3">
            <article>
                <a href="/product/detail/<%= product.id %>">
                <img src="/images/<%= product.images[0].name %>" alt="<%= product.name %>" class="pd_img-list">
                </a>
                <div class="pd_main-article">
                    <p class="pd_categoria"><%= product.brand.name %> </p>
                    <p class="pd_descripciones">
                        <a href="/product/detail/<%= product.id %>"> <%= product.name %></a>
                    </p>
                    <p class="pd_precios">$<%= product.price %></p>
                </div>
            </article>
        </div>
    <% } else if (categoria == "kids" && product.category.id == 3) { %>
        <div class="col-xs-12 col-sm-6 col-md-3">
            <article>
                <a href="/product/detail/<%= product.id %>">
                <img src="/images/<%= product.images[0].name %>" alt="<%= product.name %>" class="pd_img-list">
                </a>
                <div class="pd_main-article">
                    <p class="pd_categoria"><%= product.brand.name %> </p>
                    <p class="pd_descripciones">
                        <a href="/product/detail/<%= product.id %>"> <%= product.name %></a>
                    </p>
                    <p class="pd_precios">$<%= product.price %></p>
                </div>
            </article>
        </div>
    <% } else { %>
        <div class="col-xs-12 col-sm-6 col-md-3">
            <article>
                <a href="/product/detail/<%= product.id %>">
                <img src="/images/<%= product.images[0].name %>" alt="<%= product.name %>" class="pd_img-list">
                </a>
                <div class="pd_main-article">
                    <p class="pd_categoria"><%= product.brand.name %> </p>
                    <p class="pd_descripciones">
                        <a href="/product/detail/<%= product.id %>"> <%= product.name %></a>
                    </p>
                    <p class="pd_precios">$<%= product.price %></p>
                </div>
            </article>
        </div>
    <% } %>
<% })%> 